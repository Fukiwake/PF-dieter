.text-center.text-danger.font-weight-bold.close-food-search 閉じる
- if result.present?
  - result.each do |food|
    .d-flex.justify-content-between
      .d-flex.align-items-center
        = food["food"]
- else
  一致する食品はありません。
    .add-food-button.btn.btn-danger{"data-calorie" => "#{food["calorie"]}", "data-food" => "#{food["food"]}"} 追加
.text-center.text-danger.font-weight-bold.close-food-search 閉じる